[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"
watchPatterns = ["backend/**", "relay/**", "cfg/**", "Dockerfile", "railway.toml", "ui/**", "hub/**", "games/**", "core/**"]

[deploy]
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
