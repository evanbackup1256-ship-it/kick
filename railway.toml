[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"
watchPatterns = ["relay/**", "cfg/**", "Dockerfile", "railway.toml", "hub/**", "games/**", "bootstrap.luau", "loader.luau"]

[deploy]
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
