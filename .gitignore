node_modules/
/package.json
/package-lock.json
__pycache__/
*.pyc
.env
dist/
build/
.cache/

# Next.js / static export build outputs (generated at deploy or via relay/site postbuild)
backend/site/
relay/site/.next/
relay/site/out/
relay/site/tsconfig.tsbuildinfo

# Runtime / local artifacts (keep out of git + OneDrive sync)
*.db
supabase/.temp/
backend/data/
.alleral/
runtime_secrets.json

# Old public-repo mirror checkout. The live repo is -kick-loader.
/kick/

# Cursor IDE — local debug logs, agent sessions, temp dumps (never commit)
.cursor/

# Single public repo — no obfuscation
